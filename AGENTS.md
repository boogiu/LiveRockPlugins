# AGENTS.md

이 저장소는 **codex(Codex CLI) 전용** 플러그인 마켓플레이스다.

이 문서는 **모든 작업에 공통으로 적용되는 운영 규칙**만 담는다.
제품의 목적·동작·수용 조건, 마일스톤 계획, 작업 메모는 여기에 쓰지 않는다.

---

## 1. 권위 순서

문서·계획·코드가 서로 어긋날 때는 아래 순서가 이긴다.

1. 사용자가 현재 내린 명시적 지시
2. 제품 스펙의 목표와 수용 조건
3. 실제 코드, 매니페스트, 스키마, 테스트 결과
4. 현재 마일스톤 계획
5. 과거 계획과 작업 메모

충돌이 생기면 새 문서나 보조 문서로 봉합하지 않는다. 상위 권위에 맞춰 하위 문서를 수정하거나 폐기하고, 충돌한 가정·코드 근거·제안 변경을 함께 보고한다. 계획은 언제든 수정·폐기할 수 있으며, 구현보다 높은 권위를 갖지 않는다.

## 2. 승인 규칙

- 사용자의 명시적 승인이 없는 제안은 확정되지 않은 것으로 취급한다. 무응답, 답변 누락, 주제 전환, 다른 작업 지시는 승인이나 합의가 아니며, 미승인 제안을 실행하거나 확정 사항 및 계획의 전제로 사용하지 않는다.
- 판단이 필요한 것만 제안으로 올린다. 선택지가 둘 이상인 결정, 요청 범위를 넘는 변경,
  문서가 약속한 동작의 변경, 되돌리기 어렵거나 외부에 영향을 주는 변경이 여기 해당한다.
  검증까지 끝난 결함에 수정 방법이 하나뿐이면 실행하고 결과로 보고한다.

## 3. 문서 규칙

문서 구조를 최대한 작게 유지한다. 아래 규칙은 예외 없이 적용한다.

- 사용자의 요청 없이 새 계획서, 계약서, 추적 문서, 인수인계 문서를 만들지 않는다.
- 문서 간 섹션 ID 기반 계약이나 상호 인용 체계를 만들지 않는다.
- 설계 결정 기록(ADR)은 다음 세 조건을 **모두** 만족할 때만 작성한다.
  1. 여러 대안이 실제로 존재했다.
  2. 선택 결과가 장기간 영향을 미친다.
  3. 나중에 선택 이유를 모르면 되돌릴 위험이 크다.

## 4. 작업 범위

- 현재 task 범위를 넘어선 문제는 구현하지 않고 후속 항목으로만 보고한다.
- 변경한 모든 줄은 요청에 근거가 있어야 한다. 인접 코드·주석·포맷의 임의 개선은 하지 않는다.
- 요청한 결과를 내는 데 필요한 원인 수정은 범위에 포함한다. 요청과 무관한 기존 결함과 죽은 코드는 고치지 않고, 현재 작업의 정확성이나 검증에 영향을 주는 것만 보고한다.
- 결과와 범위가 같은 더 단순한 방법은 선택하고 결과에 적는다. 결과나 범위가 달라지는 방법은 만들기 전에 먼저 말한다.
- 일어날 수 없는 상황에 대한 에러 처리를 추가하지 않는다. 내 변경 때문에 쓰이지 않게 된 것만 제거한다.

## 5. 저장소 레이아웃

```
.agents/plugins/marketplace.json          마켓플레이스 매니페스트 (name: liverock)
plugins/<플러그인>/.codex-plugin/plugin.json   플러그인 매니페스트
plugins/<플러그인>/skills/<스킬>/SKILL.md      스킬 본문
```

- `plugin.json`에는 `skills` 필드가 있어야 스킬이 로드된다.
- 이름은 kebab-case를 쓰고, 플러그인 디렉터리 이름·`plugin.json`의 `name`·마켓플레이스
  매니페스트의 항목 이름을 전부 일치시킨다.
- 플러그인을 추가·삭제·개명하면 **매니페스트 두 개를 같은 커밋에서** 갱신한다.
- **플러그인 버전은 `main`에 머지하기 직전에 작업된 브랜치에서 올린다.** 개발 중에는 올리지 않으며, 머지 직전 검증 때 올리는 값이 곧 릴리스 버전이다. 변경 규모를 판정해 major·minor·patch 중 무엇을 올릴지와 추천 버전을 사용자에게 알리고, 승인받아 반영한다. 에이전트가 임의로 정해 올리지 않는다.
  - major: 기존 사용 방식이 깨지는 변경 (스킬 삭제·개명, 명령이나 옵션의 호환성 깨짐)
  - minor: 기능 추가 (새 스킬, 새 옵션)
  - patch: 결함 수정, 문서 수정, 내부 정리
  codex는 `plugin add` 재실행만으로 갱신된다.

## 6. 스킬 작성 규칙

- **스킬의 신규 제작과 파일 구조 변경은 codex의 `skill-creator`(`~/.codex/skills/.system/skill-creator`)로 한다.**
  스킬 폴더의 파일 목록이 바뀌는 것이 구조 변경이다 — 파일 추가·삭제·개명·이동, 본문 분리.
  파일 목록이 그대로인 본문 수정은 이 규칙의 대상이 아니다.
  `skill-creator`를 쓸 수 없는 환경에서 만들어야 하면
  [`plugins/liverock-toolkit/references/skill-authoring.md`](plugins/liverock-toolkit/references/skill-authoring.md)의
  규격만으로 진행하고, 규격 전체를 따르지 못했다는 사실을 사용자에게 알린다.
- 스킬 작성 규격(구조·분량·frontmatter·description·검수 항목)의 출처는
  `plugins/liverock-toolkit/references/skill-authoring.md` 하나다. 이 문서에 옮겨 적지 않는다.
- `description`의 트리거에는 사용자가 실제로 쓰는 한국어 표현을 넣는다.
- 개발 중에만 쓰는 스킬은 플러그인 밖 `.agents/skills/<이름>/`에 둔다. codex가 자동 로드하므로
  설치 없이 저장만으로 반영된다. 반대로 `plugins/<플러그인>/skills/` 아래 스킬은 자동 로드되지 않는다.

## 7. 코딩 규칙

- 스킬 본문에서 파일을 참조할 때는 **스킬 디렉터리 기준 상대 경로**로 지시한다. `${CLAUDE_PLUGIN_ROOT}`는 codex에 존재하지 않으므로 쓰지 않는다. 스크립트는 그 스킬 안(`skills/<name>/scripts/`)에 두고, 둘 이상의 스킬이 공유할 때만 플러그인 루트의 `scripts/`로 올린다. 스킬끼리 파일을 참조하는 상태로 남겨두지 않는다.
- 사용자 홈 경로, 드라이브 문자, 저장소 절대 경로를 하드코딩하지 않는다.
- 훅과 스크립트는 실패해도 세션을 중단시키지 않게 만든다. 기대한 입력이 없으면 조용히 통과시키고, 진단은 stderr로 남긴다.

### Windows 인코딩

- `.md`, `.json`, `.ps1`, `.py`는 UTF-8(BOM 없음)로 쓴다.
- `.bat` / `.cmd`에는 한글을 쓰지 않는다. cmd.exe가 CP949로 읽어 깨지며,
  파일 안의 `chcp 65001`로는 해결되지 않는다.
- 한글이 들어간 `.ps1`은 pwsh 7 기준으로 작성한다.

## 8. 검증 규칙

- 무엇으로 검증할지 **먼저** 정하고, 실행한 명령과 결과를 보고한다. 긴 출력은 판단에 필요한 부분만 인용한다.
- 실행 가능한 수용 조건은 문서가 아니라 테스트로 표현한다. 완료 여부는 체크박스가 아니라 실행된 코드와 검증 결과로 판단한다.
- 매니페스트를 고쳤으면 저장소 파일로 확인한다. `plugin.json`의 `name`이 디렉터리 이름과
  같고 `skills` 필드가 있는지, 마켓플레이스 매니페스트에 등록됐는지를 읽어서 확인한다.
- 설치 상태에 의존하는 것은 **설치본이 갱신된 상태에서** 확인한다. 저장소 파일만 고친 상태로는 실행 중인 세션에 반영되지 않는다.
- 테스트가 있으면 버그 수정은 재현 테스트를 먼저 만들고 통과시킨다. 리팩터링은 전후 모두 green이어야 한다.
- 어느 것도 불가능했다면 "검증하지 못했다"고 명시한다. 검증하지 않은 변경을 완료로 보고하지 않는다.

## 9. 금지 사항

- 사용자의 명시적 승인 없는 git 커밋, 스테이징, 푸시, 태그, 브랜치 삭제.
- 토큰·자격 증명·개인 식별 정보를 저장소에 커밋하는 것.
- 설치본(`~/.codex/plugins/cache` 등)을 직접 수정해 문제를 우회하는 것.
  원본은 항상 이 저장소이고, 설치본은 갱신으로만 바꾼다.
- 요청 없이 외부 서비스에 저장소 내용을 업로드하거나 배포 파이프라인을 추가하는 것.

## 10. 보고 형식

- 응답은 한국어로 쓴다. 코드, 명령, 기술 용어는 원문을 유지한다.
- 근거는 `파일:줄` 형식으로 인용한다.
- 규칙을 지키는 것으로 충분하다. 지킨다고 선언하지 않는다.
  조항 번호는 그 규칙 자체가 논의 대상일 때만 꺼낸다.

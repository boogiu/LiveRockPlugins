# 측정 기록

이 문서는 **에이전트가 자동으로 읽지 않는 자리**다. `AGENTS.md`와 `plugins/liverock-toolkit/`
아래는 기동 시 또는 스킬 발동 시 컨텍스트에 들어가므로 측정 로그를 두지 않는다.

여기에는 실제로 실행한 측정과 그 결론만 적는다. 추정은 추정이라고 표시한다.

---

## 2026-09-21 — 스킬 발동과 컨텍스트 비용

### 왜 쟀나

`agent-orchestration`을 132줄에서 462줄로 확장하면서 `description`도 작업 요청 일반에
발동하도록 넓혔다(커밋 `74aa063`). 두 변화가 곱해지면 연구원이 일을 시킬 때마다 본문이
컨텍스트에 들어간다. 월 3만원대 codex 플랜에서 이게 감당 가능한지 확인이 필요했다.

### 스킬 파일 크기 (실측)

| 파일 | 줄 | 문자 | 토큰(환산, 1tok≈1.5자) |
| --- | ---: | ---: | ---: |
| `agent-orchestration/SKILL.md` | 462 | 17,846 | ≈11.9k |
| `agent-orchestration/references/level2.md` | 557 | 21,003 | ≈14.0k |
| `agent-orchestration/references/failure-handling.md` | 260 | 8,886 | ≈5.9k |
| `agent-orchestration/references/level1.md` | 148 | 6,244 | ≈4.2k |
| `agent-orchestration/assets/review-briefing.md` | 114 | 3,032 | ≈2.0k |
| `research-orchestration/SKILL.md` | 142 | 4,510 | ≈3.0k |
| `experiment-planning/SKILL.md` | 117 | 4,397 | ≈2.9k |
| `literature-analysis/SKILL.md` | 108 | 3,194 | ≈2.1k |

`description` 길이(항상 로드되는 부분): `agent-orchestration` 511자, `research-orchestration`
350자, `literature-analysis` 302자, `experiment-planning` 217자. 합계 1,380자 ≈ 0.9k tok.

문자 수는 실측이고, **1토큰≈1.5자 환산비는 측정 D·E의 델타에서 역산한 실측값이다**(아래).
`tiktoken`은 쓰지 않았다 — D·E 델타가 실제 모델의 토크나이저와 하네스 오버헤드를 모두
포함하므로 `tiktoken`보다 정확하다.

이 문서의 초기 판본은 1토큰≈1.2자로 잡았고, 그 값은 **약 23% 과대평가**였다.

### 설치 상태 (실측)

```
PLUGIN                     STATUS              VERSION  SOURCE
liverock-toolkit@liverock  installed, enabled  0.1.0    C:\...\LiveRockPlugins\plugins\liverock-toolkit
```

`~/.codex/config.toml`의 `[plugins."liverock-toolkit@liverock"] enabled = true`. 소스가
설치본 캐시가 아니라 저장소를 직접 가리킨다.

### 측정 A·B (실측, 단 B는 설계 실패)

Paseo로 codex 에이전트 두 개를 띄워 `get_agent_status`의 `lastUsage`를 읽었다.
둘 다 `codex/gpt-5.6-terra` · `thinking low` · `auto` 모드, 같은 워크스페이스.

| | A (대조군) | B (작업 요청) |
| --- | ---: | ---: |
| 발화 | "1 더하기 1이 뭐야" | "이 저장소 문서 좀 정리해줘" |
| `inputTokens` | 22,536 | 20,918 |
| `cachedInputTokens` | 6,912 | 6,912 |
| `contextWindowUsedTokens` | 22,548 | 21,048 |
| 본문 로드된 스킬 | 없음 | 없음 |

**B가 A보다 작다. 델타가 음수다.** 스킬 본문이 안 들어갔다.

**이 측정은 설계가 잘못됐다.** B 프롬프트에 안전을 위해 넣은
`"파일은 아무것도 읽거나 수정하지 마라. 어떻게 할지 말만 하고 끝내라"`가 바로 위임 스킬이
발동하지 않게 만드는 문장이다. 측정하려던 행동을 측정 장치가 억눌렀다.

질문도 모호했다. `"본문까지 로드된 스킬"`을 물었는데, "없음"은 ①목록에조차 없었다와
②목록엔 있었지만 본문을 안 불렀다를 구분하지 못한다.

기준선 22k는 대부분 Paseo가 주입하는 MCP 도구 정의와 시스템 프롬프트로 보인다. 스킬과
무관하므로 델타를 볼 때는 상쇄된다.

### 측정 C (실측)

억제 제약을 빼고, 질문을 ①실제 호출한 스킬 ②목록에 제시된 스킬로 분리했다.
발화는 "이 저장소에 어떤 스킬들이 있는지 조사해서 정리해줘", 안전장치는
"하위 에이전트는 만들지 마라" 한 줄뿐. A와 같은 모델·thinking·모드.

| | A (대조군) | C (자연 발화) |
| --- | ---: | ---: |
| `inputTokens` | 22,536 | 45,154 |
| `cachedInputTokens` | 6,912 | 41,728 |
| `outputTokens` | 12 | 956 |
| `contextWindowUsedTokens` | 22,548 | 46,110 |
| 호출한 스킬 | 없음 | **`liverock-toolkit:agent-orchestration`** |

**스킬은 정상 발동한다.** `skill-authoring.md:191`의 "실제 발화로 발동과 결과를 확인했다"가
이것으로 충족됐다. 넓힌 `description`이 "조사해서 정리해줘"를 잡았다.

시스템 프롬프트 목록에 `liverock-toolkit`의 스킬 4개가 전부 올라와 있었다 —
`agent-orchestration`, `experiment-planning`, `literature-analysis`, `research-orchestration`.

#### 캐시가 92%다

`cachedInputTokens` 41,728 / `inputTokens` 45,154 = **92.4%**. A는 6,912/22,536 = 30.7%.

C는 여러 턴을 돌았고 A는 한 턴이었다. 세션 안에서 프리픽스(시스템 프롬프트 + 스킬 본문 +
앞 턴)가 캐시되므로, **스킬 본문은 세션당 한 번 제값을 내고 이후 턴은 캐시된다**는 뜻으로
읽힌다. 요청마다 full 과금이 아니다.

#### 본문 로드 비용은 분리하지 못했다

`contextWindowUsedTokens` 델타는 46,110 − 22,548 = **23,562**다. 그런데 C는 스킬을 부르는
동시에 매니페스트와 스킬 파일들을 실제로 읽었다. 이 델타에는 본문 로드분과 읽은 파일
내용이 섞여 있다.

추정 본문 15k + 읽은 파일 ~8k면 델타와 맞아떨어지므로 앞의 크기 추정과 모순되지는 않는다.
그러나 **분리 측정은 하지 못했다.** 스킬이 발동하면서 파일은 전혀 읽지 않는 상황을 자연스럽게
만들기 어려웠다.

#### 이득 판정 게이트가 동작한 정황

C는 "이번 요청은 파일 하나의 목록·설명 확인 작업이라 하위 에이전트 없이 직접 조사했습니다"
라고 보고했다. 위임 게이트 앞의 이득 판정이 작동한 모양이다.

**다만 이건 확정이 아니다.** 프롬프트에 "하위 에이전트는 만들지 마라"를 넣었기 때문에,
게이트 판정의 결과인지 내 제약 때문인지 분리되지 않는다.

#### 확인된 교란 요인 — 경쟁 스킬

목록에 `paseo-toolkit:agent-orchestration`도 함께 올라와 있었다. 이 PC에는
`paseo-toolkit@sky-agent-plugins`가 `installed, enabled` 상태다. 그쪽 `description`은
"모든 작업 요청에 착수하기 전에 반드시 이 스킬을 읽고 적용한다"로 더 강하다.

**이름이 같은 오케스트레이션 스킬 둘이 동시에 활성이다.** 이번엔 liverock 쪽이 불렸지만
매번 그렇다는 보장이 없다. 최종 사용자는 `paseo-toolkit`을 설치하지 않으므로 제품 결함은
아니지만, 이 PC에서 하는 모든 발동 시험의 교란 요인이다.

### 측정 D·E (실측) — 본문 로드 비용 분리

C가 분리하지 못한 값을 재려고 다시 설계했다. **둘 다 파일을 읽을 이유가 없는 질문**으로
만들어 델타에 본문분만 남겼다. 억제 문장은 쓰지 않았다.

| | D (대조군) | E (스킬 발동) | 델타 |
| --- | ---: | ---: | ---: |
| 발화 | "1기압에서 물의 끓는점이 몇 도야" | "논문 30편을 워커에 나눠 읽히려는데 몇 개로 쪼개고 동시에 몇 개를 돌릴지" | |
| `inputTokens` | 20,905 | 35,660 | **+14,755** |
| `cachedInputTokens` | 7,936 | 31,488 | |
| `outputTokens` | 25 | 397 | |
| `contextWindowUsedTokens` | 20,930 | 36,057 | +15,127 |
| 호출 스킬 | 없음 | `research-orchestration`, `agent-orchestration` | |

**본문 로드 비용 = 두 스킬 합계 ≈ 14.8k 토큰.**

E는 연구 작업 요청이라 `research-orchestration`과 `agent-orchestration`이 **둘 다** 발동했다.
이게 정상 동작이다 — 쪼개는 단위는 전자, 기동 절차는 후자 소관이다.

#### 환산비 역산

두 `SKILL.md` 합계 17,846 + 4,510 = 22,356자 ÷ 14,755토큰 = **1토큰 ≈ 1.52자**.

역방향 검산: `agent-orchestration` 11.9k + `research-orchestration` 3.0k = 14.9k ≈ 실측
14.755k. 맞아떨어진다.

E 프롬프트가 D보다 약 20토큰 길다("하위 에이전트는 만들지 마라"). 14.8k 규모에서 0.14%라
결론에 영향이 없다.

#### 기준선이 세 번 일관됐다

스킬을 부르지 않은 세 세션의 `contextWindowUsedTokens`: A 22,548 / B 21,048 / D 20,930.
20~22k 대역이 **스킬과 무관한 고정분**(Paseo가 주입하는 MCP 도구 정의 + 시스템 프롬프트 +
스킬 목록)이라는 뜻이다.

#### 실제 요청 하나가 쓰는 양

| 상황 | 고정분 | 스킬 본문 | 합계 |
| --- | ---: | ---: | ---: |
| 스킬 미발동 | 21k | 0 | **21k** |
| 연구 작업, 레벨 1 (실측 E) | 21k | 14.8k | **36k** |
| + 논문 작업이면 `literature-analysis` | 21k | 16.9k | **38k** |
| + 레벨 1 실행 기록까지 읽으면 | 21k | 21.1k | **42k** |
| 레벨 2로 판정되면 | 21k | 30.9k | **52k** |

**컨텍스트 창은 문제가 아니다.** 최악 52k는 `contextWindowMaxTokens` 258,400의 20%다.
남는 건 쿼터 소모 문제뿐이다.

---

## 미해결 — 구조상의 모순

측정 결과와 별개로, 설계 자체에 확인된 문제가 하나 있다.

위임 게이트에 "워커를 안 띄우는 것이 이득이면 팀장이 직접 한다"는 판정을 넣었다
(`research-orchestration/SKILL.md:61-67`). 이건 **워커 쿼터**를 아낀다.

그런데 그 판정을 내리려면 `SKILL.md` 본문을 이미 읽은 뒤다. "파일 하나 읽고 값 하나 꺼내기"
같은 사소한 요청에도 본문을 먼저 쓰고 나서 "이건 위임 안 하는 게 낫다"는 결론에 도달한다.
**컨텍스트 비용은 이 게이트로 아껴지지 않는다.**

원본은 넉넉한 환경(동시 워커 10)을 전제로 쓰였다. 이식하면서 워커 수(10→2)와 검토
라운드(3→2)는 줄였지만 컨텍스트 크기는 줄이지 않았다.

### 검토 중인 방향

사용자는 엄격한 오케스트레이션을 유지하기로 했고, 2단 구조를 선호한다고 밝혔다.

| 방향 | 대가 |
| --- | --- |
| `description` 좁히기 | 위임 게이트를 사실상 포기 |
| 본문을 더 쪼개기 | `skill-authoring.md:5절`이 실행 경로·권한·승인·성공 여부를 정하는 지시는 본문에 남기라고 해서 한계가 있다 |
| **2단 구조** — 게이트만 담은 얇은 스킬이 먼저 발동하고, 위임이 필요하다고 판정됐을 때만 두꺼운 본문을 읽는다 | 구조 변경이 크다. 위 모순을 정면으로 없애는 유일한 방법 |
| 그대로 두고 실측 후 판단 | 문제가 없으면 아무것도 안 해도 된다 |

### 측정 이후의 판단

측정이 세 가지를 확정했다.

1. **발동은 된다.** 2단 구조를 짜도 안 뜰 걱정은 없다.
2. **본문 로드 비용은 14.8k이지 22k가 아니다.** 초기 추정이 23% 부풀려져 있었다.
3. **캐시가 88~92%다.** 세션 안에서 프리픽스가 캐시되므로 본문은 세션당 한 번 제값을 낸다.

2번과 3번이 2단 구조의 근거를 약하게 만든다. 본문이 매 요청 비용이면 시급하지만, **세션당
한 번 14.8k**면 급하지 않다. 구조상 모순("사소한 요청에도 본문을 먼저 읽는다")도 세션 첫
요청에만 해당한다.

**그래서 지금은 2단 구조로 가지 않는 쪽을 권한다.** 다만 아래 두 값이 나오면 뒤집힌다.

- **codex 플랜의 쿼터가 캐시된 토큰을 어떻게 세는지.** 캐시 히트를 full로 세면 88%는 위안이
  되지 않는다.
- **연구원이 세션을 얼마나 자주 새로 여는지.** 요청마다 새 세션이면 캐시가 매번 비고,
  세션당 한 번이 곧 요청당 한 번이 된다.

두 번째가 특히 중요하다. 작업 하나 끝날 때마다 창을 닫는 습관이면 캐시율은 측정 환경의
산물일 뿐이고, 실제 비용은 **요청마다 14.8k**가 된다. 그때는 2단 구조가 확실한 이득이다.

두 대조군의 캐시율(A 30.7%, D 38.0%)이 단일 턴 세션의 값이다. 세션을 새로 열면 이 수준에서
시작한다는 뜻으로 읽힌다.

---

## 아직 검증하지 못한 것

- **이득 판정 게이트의 단독 동작.** C에 넣은 "하위 에이전트 금지" 제약과 분리되지 않았다.
  제약 없이 사소한 작업을 시켜 직접 처리하는지 봐야 한다.
- **codex 플랜의 쿼터가 캐시된 토큰을 어떻게 세는지.** 조회로 확인되지 않는다.
- **연구원이 세션을 새로 여는 빈도.** 캐시율이 실제 비용에 반영되는지가 여기 걸린다.
- **`paseo-toolkit`을 비활성화한 상태의 발동.** 경쟁 스킬이 없을 때도 같은지 확인 안 됨.
- **`references/` 파일의 실제 로드 비용.** 표의 값은 문자 수를 실측 환산비로 돌린 것이고,
  레벨 2가 실제로 돌아간 세션에서 재지 않았다.

뒤의 두세 번째가 없으면 "실제 비용이 얼마인가"에 답할 수 없다. 쿼터 계산 방식은 플랜 문서,
세션 습관은 사용자만 아는 값이다.

**해결됨**: 본문 로드 비용의 분리 측정(D·E), 실제 발화 발동 확인(C),
토큰 환산비(D·E 역산).

## 2026-09-21 — 격리 환경 실제 실행

### 격리 방법

`CODEX_HOME` 환경변수를 바꾸면 codex의 설정·플러그인·인증·세션이 통째로 그 디렉터리로
간다. 실제 홈은 건드려지지 않는다. 이것으로 **다른 플러그인의 영향 없이** 시험할 수 있다.

```text
테스트 디렉터리\
├── repo\         개발 저장소의 클론 (origin = 로컬 개발 경로)
├── codex-home\   격리된 CODEX_HOME
└── sync.ps1      가져오기 + 재설치
```

`origin`을 로컬 개발 저장소로 두면 푸시 없이 커밋만으로 동기화된다. 커밋 안 한 작업은
넘어가지 않는데, 이건 의도다 — 시험 대상이 편집기에 떠 있는 것이 아니라 커밋된 것이어야 한다.

격리 확인: 테스트 홈에는 `liverock-toolkit` 하나만, 실제 홈에는 `paseo-toolkit`을 포함해
6개가 그대로 있었다. 앞선 측정에서 교란 요인이던 경쟁 스킬이 사라졌다.

### 스킬 발동 확인 (실측) — 점검표 11번 해소

격리 홈에서 codex를 띄우고 `처음 설치했어. 뭐부터 해야 해?`를 입력했다.

```text
Read SKILL.md (liverock-toolkit:onboarding skill)
python .../onboarding/scripts/check_env.py
| # | 항목 | 결과 | 내용 | 다음 동작 |
  codex  : ...\npm\codex.cmd  (PATH)
  claude : ...\.local\bin\claude.exe  (PATH)
```

`skill-authoring.md:191`의 "실제 발화로 발동과 결과를 확인했다"가 이것으로 충족됐다.
스킬이 발동했고, 본문이 지시한 스크립트를 지시한 경로로 호출했고, provider CLI 경로까지
냈다. 이 항목은 `0.2.0` 내내 미검증으로 남아 있었다.

### 새로 드러난 결함 2건

**결함 A — 온보딩에 codex 로그인 점검이 없다.**

격리 홈에서 codex를 처음 띄우니 로그인 화면이 떴다. 인증은 `auth.json`에 있고 홈마다
따로다. 그런데 온보딩 9항목에 로그인 확인이 없다(`SKILL.md`·`check_env.py` 양쪽 grep 0건).
1번(플러그인 설치·활성)은 **이미 로그인된 상태를 전제**한다.

연구원이 codex를 새로 깔면 이 화면부터 만난다. 플러그인 설치보다 앞이다.

**결함 B — 플러그인에 닿기 전에 첫 실행 확인 화면이 넷이다.**

순서대로 이렇다.

1. 로그인 (ChatGPT / Device Code / API key)
2. 업데이트 안내
3. 디렉터리 신뢰 여부
4. 샌드박스 설정 (기본은 **관리자 권한 필요**, 비관리자 선택지 있음)

넷 다 codex CLI를 아는 사람에게는 당연하지만, 연구원에게는 각각이 멈춤 지점이다. 특히
4번은 관리자 권한을 묻는다. 온보딩이 이 넷을 전혀 다루지 않는다.

그리고 비관리자 샌드박스를 고르면 **명령마다 승인 프롬프트가 뜬다.** 이번 시험에서도
`Get-Content`·`rg`·`git status`·`python` 각각에 승인이 필요했다. 사용자가 보고한
"오류 메시지에 당황"과 같은 계열의 마찰이다.

### 연구 작업 실제 실행 (실측)

격리 환경에서 실제 연구 주제로 돌렸다. 발화는
`고분자 전해질 연료전지 금속 분리판 부식에 대한 논문 찾아서 정리해줘`.

승인 프롬프트를 없애려고 `-a never -s workspace-write -c
sandbox_workspace_write.network_access=true`로 띄웠다. 쓰기는 테스트 클론으로 제한되고
네트워크는 열린다. `danger-full-access`는 쓰지 않았다.

결과:

- **스킬 3개가 발동했다** — `literature-analysis`, `research-orchestration`,
  `agent-orchestration`. 앞선 측정 E에서 연구 요청에 오케스트레이션 둘이 같이 뜬 것과 일치한다.
- `search_papers.py`를 영어 검색어로 2회 실행해 실제 논문을 받았다. DOI가 실재하고
  저널(`Int. J. Hydrogen Energy`, `Energy and AI`)도 이 분야의 것이 맞다.
- **논문 5편을 제시하고 사용자 선택을 기다렸다.** `literature-analysis` 2절의 "사용자가
  고르기 전에 분석으로 넘어가지 않는다"를 지켰다.
- **공개 전문과 초록·서지정보를 구분해 표시했다.** 같은 스킬 1절의 "`oa_url`이 비어 있으면
  초록까지만 확인된 것이고 그 사실을 알린다"를 지켰다.

미검증: 사용자가 번호를 고른 뒤의 정독·정리 단계는 돌리지 않았다.

### 결함 C — 스크립트 출력이 UTF-8이 아니었다

연구 시험 화면에 표 머리글이 `| �ŷ� | ���� |`로 깨져 나왔다. 출력 바이트를 확인하니
`bdc5 b7da` — **CP949로 인코딩된 "신뢰"**였다.

Windows에서 Python은 stdout에 로캘 인코딩을 쓴다. UTF-8을 기대하는 쪽(에이전트 화면, 로그,
`--json` 파이프)이 전부 깨진다. `check_env.py`·`check_registration.py`에는
`reconfigure(encoding="utf-8")`가 있었고 `search_papers.py`에만 없었다.

같은 방식으로 고쳤다. 첫 바이트가 `ec8b a0`(UTF-8 "신")으로 바뀌고 표가 정상 출력되며
`--json`이 파싱된다.

**연구원이 논문 검색을 처음 돌리면 바로 보는 화면**이다.

### 한계

**Paseo 앱은 격리되지 않는다.** `CODEX_HOME`은 codex CLI 상태만 가른다. 프로필·워크스페이스는
이 PC에 하나뿐이라, 프리셋을 `--apply`하면 실제 프로필이 바뀐다. 프리셋 등록 시험은
백업·`--rollback` 확인이 먼저다.

## 측정 비용

워커 5개(A·B·C·D·E). B는 설계 실패로 폐기. 전부 `codex/gpt-5.6-terra` · `thinking low` ·
`auto` 모드, 같은 워크스페이스.

설계 교훈: **측정 대상을 억누르지 않는 안전장치를 골라야 한다.** B는 안전을 위해 넣은
"파일 읽지 마라 + 말만 하고 끝내라"가 위임 스킬의 발동 자체를 막아 무효가 됐다. D·E는
금지 대신 **파일이 필요 없는 질문**을 골라 같은 안전을 얻으면서 측정을 살렸다.

## 2026-09-23 — codex 런타임의 도구 표면과 위임 게이트

### 왜 쟀나

연구원이 "오케스트레이션 폴더가 비어 있다"고 보고했다. 워커는 뜨는데 `.agents/orchestration/`
아래에 `GRAPH.md`가 생기지 않는 상태다.

### 실측 1 — codex 세션에는 Paseo MCP 도구가 없다

Paseo가 띄운 codex 에이전트에 도구 목록을 직접 물었다. 받은 전체 목록:

```text
functions.exec, functions.wait,
collaboration.spawn_agent, collaboration.followup_task, collaboration.send_message,
collaboration.interrupt_agent, collaboration.list_agents, collaboration.wait_agent,
mcp__cua_repl.js, mcp__cua_repl.js_reset
```

`create_agent`·`list_workspaces`·`list_agents`·`get_agent_status`·`send_agent_prompt`·
`cancel_agent`·`list_profiles`·`list_providers` **전부 없음**으로 확인됐다.

`~/.paseo/config.json`의 `daemon.mcp.injectIntoAgents`는 **`true`였다.** 켜져 있는데도
도구가 없다. 이 설정은 codex 세션에 MCP를 주입하는 경로가 아니다.

**결과**: `agent-orchestration`은 존재하지 않는 도구를 부르라고 지시하고 있었다. 팀장은
`collaboration.spawn_agent`로 조용히 우회했고, 기록 지시가 기동 호출에 묶여 있어 `GRAPH.md`가
통째로 빠졌다. 이것이 연구원이 본 증상이다.

`paseo` CLI는 정상 동작한다. 데몬이 `127.0.0.1:6767`에 있고 `run`·`ls`·`inspect`·`send`·
`stop`·`wait`·`workspace ls`·`provider ls`가 모두 있으며 `--json`이 파싱된다.
**`paseo profile` 하위 명령은 없다**(`error: unknown command 'profile'` 재현). 프로필 조회는
CLI로 불가능하다.

### 실측 2 — 설치본은 캐시에서 로드된다 (앞 기록 정정)

2026-09-21 기록의 "소스가 설치본 캐시가 아니라 저장소를 직접 가리킨다"는 `plugin list`의
`SOURCE` 열을 읽은 것이고, **실제 로드 경로는 캐시다.** 팀장의 활동 기록에 그대로 남았다 —
`Get-Content -Raw '~/.codex/plugins/cache/liverock/liverock-toolkit/0.2.1/skills/...'`.

저장소만 고친 상태로 돌린 시험 두 건이 이것 때문에 무효가 됐다.

`codex plugin add liverock-toolkit@liverock` 재실행으로 **같은 버전에서도 캐시가 갱신된다**
(462줄 → 483줄 확인). `--force` 플래그는 없고 필요하지도 않다. 갱신 후 `diff -r` 차이 0건.

### 실측 3 — 직접 처리가 팀장 컨텍스트를 더 먹는다

위임 게이트에 걸려 팀장이 직접 처리한 실행에서, 팀장이 읽은 파일의 실측 크기:

| 파일 | 자 |
| --- | ---: |
| `onboarding/SKILL.md` | 21,358 |
| `experiment-planning/SKILL.md` | 10,240 |
| `literature-analysis/SKILL.md` | 6,701 |
| 합계 | 38,299 ≈ **25.5k 토큰** |

위임했다면 팀장 컨텍스트에 들어올 것은 브리핑(템플릿 2,076자 + 작업 내용)과 완료 알림
요약뿐이다 — 대략 **7k 토큰**. `SKILL.md` 7절이 "결과 파일 본문을 네 컨텍스트로 옮기지
않는다"고 막아 둔 덕이다.

**팀장 컨텍스트 기준 직접 처리가 3.6배 비싸다.** 다만 총 토큰은 반대다 — 워커마다 고정분
21k를 새로 무므로 워커 3개면 직접 46.5k 대 위임 91k다.

| 보는 축 | 싼 쪽 |
| --- | --- |
| 단발 요청의 총 토큰 | 직접 |
| 세션 누적 · 팀장 컨텍스트 | 위임 |

팀장 컨텍스트는 프리픽스에 박혀 이후 모든 턴이 끌고 가고, 워커 컨텍스트는 한 번 쓰고
사라진다. 쿼터를 어느 축으로 세는지는 여전히 미확인이다.

### 관측 — 위임 게이트가 과발동한다

격리 환경에서 두 번 돌렸다. 둘 다 `GRAPH.md`가 생기지 않았다.

| | 발화 | 결과 |
| --- | --- | --- |
| 검증 1 | 스킬 3개의 description 규격 점검 | 위임하지 않고 직접 처리 |
| 검증 2 | 같은 성격 + `셋을 워커에 나눠서 병렬로 돌려줘` | `collaboration.spawn_agent`로 워커 3개 기동, 기록 0 |

검증 2는 `description`이 명시적 위임 트리거로 적어 둔 표현("나눠서 돌려")을 그대로 썼는데도
Paseo 경로를 타지 않았다.

### 구조 결함 — 게이트 조건 ③과 2절 Step 1이 충돌한다

같은 상황에 정반대 처방이 있었다.

| 어디 | 조건 | 처방 |
| --- | --- | --- |
| 2절 판정 트리 Step 1 | 어떤 작업의 입력이 다른 작업의 출력인가 | 레벨 2 (그래프 실행) |
| 위임 게이트 ③ | 앞 작업 결과가 있어야 시작하는데 그 앞이 아직인 일 | 팀장이 직접 한다 |

게이트가 2절보다 **앞에** 있으므로 게이트가 이긴다. 의존이 보이는 순간 레벨 2로 내려가지
못한다. 레벨 2가 한 번도 실행되지 않은 것과 맞는 자리다.

게이트 조건 ②("결과를 받아도 어차피 다시 검토해야 하는 일")는 방향이 거꾸로다. 검토가
필요하다는 것은 대상이 크다는 뜻이고, 크면 직접 읽는 비용이 더 크다(실측 3). 게다가 8절이
모든 워커 결과를 검토하게 하므로 모든 위임이 이 조건에 해당한다.

쿼터 통제는 게이트가 아니라 `research-orchestration` 1절의 동시 2·총 5 상한이 한다.

### 측정 함정 — PowerShell `Measure-Object -Line`은 빈 줄을 세지 않는다

`(Get-Content $p | Measure-Object -Line).Lines`가 `wc -l`보다 작게 나온다. 실측 두 건:

| 파일 판본 | `wc -l` | `Measure-Object -Line` | 차 = 빈 줄 수 |
| --- | ---: | ---: | ---: |
| `agent-orchestration/SKILL.md` (수정 전) | 462 | 350 | 112 |
| 같은 파일 (수정 후) | 483 | 367 | 116 |

줄 수를 합격 기준으로 쓰는 점검(`skill-authoring.md`의 500줄 상한)에서 이 차이로 통과·실패가
갈릴 수 있다. 에이전트가 낸 줄 수가 기대와 다르면 지어낸 값이라고 판단하기 전에 어느 방법으로
셌는지부터 본다.
